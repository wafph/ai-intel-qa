# 问题反馈前端升级记录

## 本次新增

- 在左侧用户菜单中增加“问题反馈”，位置在“我的收藏”上方；
- 新增问题反馈弹窗，所属模块和问题描述必填；
- 上传截图、联系电话、邮箱不显示“选填”字样；
- 支持智能问答、智能检索、辅助起草、合规审核、其它五种模块；
- 支持最多 5 张 JPG、PNG、WebP 截图；
- 使用当前智能体登录凭证向 `/v1/feedback` 提交；
- 自动携带当前会话、页面路由和功能页上下文；
- 提交成功后由独立反馈管理端查询、处理和导出。

## 未修改范围

- 四个智能体功能页业务逻辑；
- 会话、历史、收藏、置顶和标题编辑；
- 流式输出、任务恢复和停止生成；
- 文档上传、PDF 解析、原文标记、预览和导出；
- 智能问答原有点赞及点踩反馈；
- 当前认证模式和其他接口配置。

## 构建结果

已执行：

```bash
npm run build
```

TypeScript 检查和 Vite 生产构建均通过，交付包包含可直接发布的 `dist/`。
