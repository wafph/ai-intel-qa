# v12-2-15 前端最小增量：审核 PDF 原文定位顶部对齐

## 修改目标

合规审核点击“原文定位”后，右侧 PDF 原文定位面板的上沿与左侧历史面板搜索框上沿保持一致。

## 修改范围

仅修改 `src/views/ComplianceReview.vue` 中 `with-original-panel` 布局样式：

- 将分栏容器顶部内边距从 `0` 调整为 `12px`；
- 同步将左侧会话历史在分栏状态下的顶部 padding 从 `12px` 调整为 `0`，避免内容整体二次下移；
- 增加 `box-sizing: border-box`，避免 padding 参与高度计算造成面板溢出。

## 未改动内容

- 未修改 PDF detect / prepare / 原文定位业务逻辑；
- 未修改问答、检索、起草逻辑；
- 未修改后端接口；
- 未修改输入框、历史折叠、复制等逻辑。
